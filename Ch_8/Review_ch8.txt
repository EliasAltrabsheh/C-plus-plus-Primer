1) What kind of functions are good candidates for incline function?
Short and non recursive functions.

2) default value of prototype declaration:

a) void song(char * name, int times = 1);
b)void song(char * name = 'o, My papa', int times = 1);

3) a question about writing an overloading ?

void iquote(int x)
{
  cout << "\ "" << x << endl;

}
void iquote(long y)
{
  cout << "\ "" << y << endl;

}
void iquote(char * str x)
{
  cout << "\ "" <<str << endl;

}
